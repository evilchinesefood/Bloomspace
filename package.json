{
  "name": "bloomspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Eufloria-style RTS — buildless vanilla ES-module PWA (three.js + bloom). Dev tooling only; not shipped.",
  "scripts": {
    "test": "node --test",
    "format": "prettier --write \"**/*.{js,html,css,json}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "prettier": "^3.3.0"
  }
}
